export interface UserData {
    id: string
    name: string
    dob: string
    age: string
    address: string
  }
  
  export interface UserResponse {
    success: boolean
    error?: string
  }
  
  